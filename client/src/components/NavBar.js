import React, { useContext, useEffect } from 'react';
import { Box, Typohtaphy } from "@material-ui/core";
import { MainContext } from "../MainContext";
import { makeStyles } from "@material-ui/core/styles";


const NavBar = () => {
    return (
        <Box></Box>
    )
};

export default NavBar;